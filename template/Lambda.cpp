#include <bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int> a{1, 27, 13, 41, 59, 84, 21, 35, 65, 10};
    // sort(a.begin(), a.end(), [](int a, int b)
    //      { return a > b; });
    // for (int x : a)
    //     cout << x << " ";
    // int N=100,M=10;
    // auto f = [&N,M](int a, int b)
    // {   N=10;
    //     return (a+b)*M; };
    // cout <<endl<< f(8, 10) << " " <<N;

    // string a = "abcd";
    // next_permutation(a.begin(), a.end());
    // do
    // {
    //     for(auto x:a)
    //         cout << x << " ";
    //     cout << endl;
    // } while (next_permutation(a.begin(),a.end()));
    // vector<int> a{1,1,2,2,3,3,4,5};
    // a.erase(unique(a.begin(), a.end()),a.end());
    // for (int x : a)
    //     cout << x << " ";
}